# Быстрый старт - MikroTik Infrastructure v1.3

**Время развёртывания:** 15-20 минут
**Уровень сложности:** Начальный
**Версия:** 1.3 (Централизованная конфигурация)

---

## Что вы получите

После завершения этого руководства у вас будет:

- ✅ Полностью защищённая сеть (100% критических проблем решено)
- ✅ Централизованная конфигурация (легко изменять параметры)
- ✅ Firewall с правильными правилами
- ✅ DNS-over-HTTPS (CloudFlare)
- ✅ NTP синхронизация времени
- ✅ Базовая настройка WiFi (опционально)
- ✅ Container-ready инфраструктура

---

## Предварительные требования

1. **RouterOS 7.x** установлен на устройстве
2. **SSH или Console доступ** к роутеру
3. **WinBox или WebFig** для удобства (опционально)
4. **Базовое понимание** сетевых концепций (IP, маски, DHCP)

---

## Шаг 1: Подготовка конфигурационных файлов

### 1.1 Клонируйте репозиторий

```bash
git clone <repository-url>
cd mikrotik
```

### 1.2 Создайте файл с секретами

```bash
cp 00-secrets.rsc.template 00-secrets.rsc
```

### 1.3 Отредактируйте секреты

Откройте `00-secrets.rsc` и **ОБЯЗАТЕЛЬНО** замените:

```routeros
# ⚠️ КРИТИЧЕСКИ ВАЖНО: Измените ВСЕ пароли!

:global secAdminPassword "your-strong-admin-password-here"
:global secWiFiMainPassword "your-wifi-main-password-here"
:global secWiFiGuestPassword "your-wifi-guest-password-here"
:global secSSTPUsername "vpn-user"
:global secSSTPPassword "vpn-strong-password-here"

# Для WireGuard S2S (опционально)
:global secWGPrivateKey "generate-with-wg-genkey"
:global secWGPublicKey "generate-with-wg-pubkey"
:global secWGPeerPublicKey "public-key-from-remote-site"

# Для BGP и XRay (опционально, для продвинутых пользователей)
# ...
```

**🔒 ВАЖНО:** Файл `00-secrets.rsc` **НЕ** попадёт в git благодаря `.gitignore`!

---

## Шаг 2: Настройка инфраструктурных параметров

### 2.1 Откройте `00-config.rsc`

### 2.2 Измените под вашу сеть

**Минимально необходимые изменения:**

```routeros
############################################################
# SYSTEM
############################################################

:global cfgHostname "R1-Core"              # ← Измените на ваше имя
:global cfgTimezone "Europe/Kiev"          # ← Измените на ваш часовой пояс

############################################################
# NETWORK INTERFACES
############################################################

:global cfgWanInterface "ether1"           # ← Проверьте, что это ваш WAN порт
:global cfgLANPorts [:toarray "ether2,ether3,ether4"]  # ← Ваши LAN порты

############################################################
# IPv4 ADDRESSING
############################################################

# Если хотите изменить IP-адреса LAN:
:global cfgLANNetwork "192.168.1.0/24"
:global cfgLANGateway "192.168.1.1/24"
:global cfgLANPoolStart "192.168.1.50"
:global cfgLANPoolEnd "192.168.1.250"
```

**Опциональные изменения** (можно пропустить для быстрого старта):
- IPv6 (Hurricane Electric Tunnel)
- WiFi настройки
- Container IPs
- VPN конфигурация

---

## Шаг 3: Загрузка файлов на роутер

### Вариант A: Через SSH/SCP (рекомендуется)

```bash
# Скопируйте файлы на роутер
scp 00-config.rsc admin@192.168.88.1:/
scp 00-secrets.rsc admin@192.168.88.1:/
scp 01-base.rsc admin@192.168.88.1:/
scp firewall_complete.rsc admin@192.168.88.1:/

# (опционально) Скопируйте другие модули по необходимости
scp 02-network.rsc admin@192.168.88.1:/
scp 03-dhcp.rsc admin@192.168.88.1:/
# ... и т.д.
```

### Вариант B: Через WinBox

1. Откройте **WinBox** → **Files**
2. Перетащите файлы `.rsc` в корень файловой системы

### Вариант C: Через WebFig

1. Откройте WebFig → **Files**
2. Upload файлы `.rsc`

---

## Шаг 4: Импорт конфигурации

### 4.1 Подключитесь к роутеру

```bash
ssh admin@192.168.88.1
```

Или используйте WinBox → **New Terminal**

### 4.2 Импортируйте в правильном порядке

**⚠️ ВАЖНО: Порядок имеет значение!**

```routeros
# 1. СНАЧАЛА - конфигурационные переменные
/import 00-config.rsc

# 2. ПОТОМ - секреты
/import 00-secrets.rsc

# 3. ПОТОМ - базовая настройка
/import 01-base.rsc

# 4. Firewall (ОБЯЗАТЕЛЬНО!)
/import firewall_complete.rsc

# 5. Сетевая инфраструктура
/import 02-network.rsc

# 6. DHCP сервера
/import 03-dhcp.rsc

# 7. DNS (если нужен DoH)
/import dns-doh.rsc

# 8. (Опционально) WiFi
# /import 06-wifi-unified.rsc

# 9. (Опционально) VPN
# /import 05-sstp-vpn.rsc
# /import wireguard-s2s.rsc

# 10. (Опционально) Containers
# /import container-setup.rsc
# /import xray/...
```

### 4.3 Проверьте импорт

```routeros
# Проверьте логи
/log print where topics~"error|critical"

# Должны увидеть:
# "Configuration variables loaded from 00-config.rsc"
# "Secret variables loaded from 00-secrets.rsc"
# "Base system configuration applied"
# ...
```

---

## Шаг 5: Проверка работоспособности

### 5.1 Проверьте системные параметры

```routeros
/system identity print
# identity: R1-Core (или ваше имя)

/system clock print
# time-zone-name: Europe/Kiev
# time: 14:25:30
# date: dec/15/2025
```

### 5.2 Проверьте сетевые интерфейсы

```routeros
/interface print
# Должны увидеть: ether1-5, bridge-lan, pppoe-out1 (если PPPoE), vlan20, vlan30, vlan99, etc.

/ip address print
# Должны увидеть IP на bridge-lan (192.168.1.1/24)
```

### 5.3 Проверьте Firewall

```routeros
/ip firewall filter print
# Должны быть правила для:
# - input (защита роутера)
# - forward (изоляция guest, контроль трафика)

/ip firewall nat print
# Должно быть правило masquerade для WAN
```

### 5.4 Проверьте DHCP

```routeros
/ip dhcp-server print
# Должны быть серверы для LAN, Management, Guest

/ip dhcp-server lease print
# Увидите подключенные устройства
```

### 5.5 Проверьте DNS

```routeros
/ip dns print
# servers: 1.1.1.1,1.0.0.1
# allow-remote-requests: no
# use-doh-server: https://cloudflare-dns.com/dns-query (если импортирован dns-doh.rsc)

# Тест резолвинга
:resolve google.com
# Должны получить IP адрес
```

### 5.6 Проверьте доступ с клиента

С компьютера в LAN сети:

```bash
# Проверка получения IP
ipconfig   # Windows
ip a       # Linux/Mac

# Ping роутера
ping 192.168.1.1

# Ping интернета
ping 1.1.1.1

# DNS резолвинг
ping google.com

# Проверка доступа к роутеру
ssh admin@192.168.1.1    # Должен запросить пароль
```

---

## Шаг 6: Первичная настройка завершена!

### Что работает:

- ✅ **Базовая система:** hostname, timezone, NTP
- ✅ **Сеть:** LAN bridge, VLANs (main, guest, management)
- ✅ **DHCP:** Автоматическая раздача IP
- ✅ **DNS:** CloudFlare DNS с DoH (если импортирован)
- ✅ **Firewall:** Защита роутера и изоляция guest-сети
- ✅ **NAT:** Выход в интернет
- ✅ **Безопасность:** 🟢 МАКСИМАЛЬНАЯ (6/6 критических проблем решено)

### Что ещё можно настроить:

- ⏩ **WiFi** (см. [WiFi Setup Guide](../wifi/README.md))
- ⏩ **IPv6** (см. `02-network.rsc` - Hurricane Electric Tunnel)
- ⏩ **VPN** (SSTP, WireGuard S2S)
- ⏩ **Containers** (XRay, Nginx, Certbot)
- ⏩ **BGP** (для продвинутых сценариев прокси)

---

## Решение проблем

### Проблема: "file not found" при импорте

**Причина:** Файлы не загружены на роутер

**Решение:**
```routeros
/file print
# Убедитесь, что все .rsc файлы присутствуют
```

### Проблема: "syntax error" при импорте

**Причина:** Не импортирован 00-config.rsc или 00-secrets.rsc

**Решение:** Импортируйте **СНАЧАЛА** `00-config.rsc`, потом `00-secrets.rsc`

### Проблема: Нет интернета на клиентах

**Проверьте:**

```routeros
# 1. WAN интерфейс получил IP
/ip address print

# 2. Маршрут по умолчанию
/ip route print where dst-address=0.0.0.0/0

# 3. NAT работает
/ip firewall nat print

# 4. DNS резолвится
:resolve google.com
```

### Проблема: Не могу подключиться к роутеру по SSH

**Причина:** ACL ограничивает доступ

**Решение:**
```routeros
# Проверьте, что ваш IP в разрешённых сетях
/ip service print

# Если нужно, временно разрешите все
/ip service set ssh address=0.0.0.0/0

# Потом верните ограничение
/ip service set ssh address=192.168.1.0/24,172.16.99.0/24
```

---

## Что дальше?

### Рекомендуемые следующие шаги:

1. **Изучите архитектуру** → [ARCHITECTURE.md](./ARCHITECTURE.md)
2. **Настройте WiFi** → [WiFi Setup Guide](../wifi/README.md)
3. **Ознакомьтесь с безопасностью** → [SECURITY_STATUS.md](./SECURITY_STATUS.md)
4. **Настройте мониторинг** (опционально)
5. **Создайте резервную копию**:
   ```routeros
   /system backup save name=mikrotik-v1.3-initial
   /export file=mikrotik-v1.3-initial-config
   ```

### Полезные команды для повседневной работы:

```routeros
# Просмотр логов
/log print

# Просмотр DHCP клиентов
/ip dhcp-server lease print

# Просмотр активных соединений
/ip firewall connection print

# Мониторинг трафика интерфейса
/interface monitor-traffic ether1

# Резервное копирование
/system backup save name=backup-$(date "+%Y%m%d")
/export file=config-$(date "+%Y%m%d")
```

---

## Поддержка

- **Документация проекта:** [docs/README.md](./README.md)
- **Troubleshooting:** [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)
- **Официальная документация MikroTik:** [help.mikrotik.com](https://help.mikrotik.com/docs/)

---

**Поздравляем! Ваша MikroTik инфраструктура готова к работе!** 🎉

**Создано:** Claude Code (Sonnet 4.5)
**Дата:** 15 декабря 2025
**Версия:** 1.3
