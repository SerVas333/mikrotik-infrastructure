# Руководство по развёртыванию - MikroTik Infrastructure v1.3

**Целевая аудитория:** Системные администраторы
**Уровень сложности:** Средний/Продвинутый
**Версия:** 1.3 (Централизованная конфигурация)
**Время развёртывания:** 30-90 минут (в зависимости от сценария)

---

## 📋 Содержание

1. [Подготовка к развёртыванию](#подготовка-к-развёртыванию)
2. [Сценарии развёртывания](#сценарии-развёртывания)
3. [Пошаговая инструкция](#пошаговая-инструкция)
4. [Проверка после развёртывания](#проверка-после-развёртывания)
5. [Rollback план](#rollback-план)
6. [Обновление с предыдущих версий](#обновление-с-предыдущих-версий)

---

## Подготовка к развёртыванию

### 1.1 Системные требования

#### Минимальные требования RouterOS:
- **RouterOS 7.x** (рекомендуется 7.13+ для безопасности)
- **Архитектура:** ARM, ARM64, MIPSBE, x86, или x86_64
- **RAM:** Минимум 128 MB (рекомендуется 256+ MB для containers)
- **Storage:** Минимум 16 MB свободно (рекомендуется 64+ MB для containers)

#### Поддерживаемые устройства (примеры):
- ✅ hEX series (RB750Gr3, hEX S)
- ✅ hAP series (hAP ac2, hAP ac3)
- ✅ CCR series (для enterprise)
- ✅ RB2011 series
- ✅ CHR (Cloud Hosted Router)

#### Проверка версии RouterOS:
```routeros
/system resource print
# Убедитесь: version >= 7.0
```

### 1.2 Предварительные требования

#### Сеть и доступ:
- ✅ Физический или SSH доступ к роутеру
- ✅ WinBox установлен (опционально, но рекомендуется)
- ✅ Доступ к WAN подключению (для тестирования интернета)
- ✅ Резервная копия текущей конфигурации (если обновление)

#### Инструменты:
- ✅ Git клиент (для клонирования репозитория)
- ✅ Текстовый редактор (VSCode, Sublime, nano, vim)
- ✅ SCP/SFTP клиент (или используйте встроенный scp)

#### Информация для настройки:
- ✅ Ваша сетевая схема (IP адреса, подсети, VLANs)
- ✅ Список физических интерфейсов (какой WAN, какие LAN)
- ✅ Пароли для WiFi, VPN, admin (сгенерируйте заранее!)
- ✅ (Опционально) IPv6 prefix от провайдера или HE Tunnel

### 1.3 Создание резервной копии (ОБЯЗАТЕЛЬНО!)

**Перед любым изменением конфигурации:**

```routeros
# Резервная копия конфигурации
/system backup save name=backup-before-v1.3

# Экспорт текущей конфигурации
/export file=export-before-v1.3

# Скачайте файлы на ваш компьютер через WinBox → Files
# Или через SCP:
# scp admin@192.168.88.1:/backup-before-v1.3.backup ./
# scp admin@192.168.88.1:/export-before-v1.3.rsc ./
```

**💾 Храните резервные копии в безопасном месте!**

---

## Сценарии развёртывания

### Сценарий A: Новая установка (Clean Install)

**Применимо когда:**
- Новое устройство MikroTik
- Чистая установка RouterOS
- Нет существующей конфигурации

**Время:** ~30 минут

**Шаги:**
1. Базовая настройка устройства (IP, доступ)
2. Загрузка конфигурационных файлов
3. Настройка `00-config.rsc` и `00-secrets.rsc`
4. Импорт модулей

---

### Сценарий B: Обновление с v1.2

**Применимо когда:**
- У вас развёрнута v1.2 (firewall_complete)
- Хотите перейти на централизованную конфигурацию v1.3

**Время:** ~45 минут

**Шаги:**
1. Резервное копирование
2. Создание `00-config.rsc` на основе текущих настроек
3. Создание `00-secrets.rsc`
4. Замена модулей на новые версии
5. Тестирование

---

### Сценарий C: Обновление с v1.0/1.1

**Применимо когда:**
- У вас развёрнута старая версия (v1.0 или v1.1)
- Много несовместимых изменений

**Время:** ~60-90 минут

**Рекомендация:** Рассмотрите чистую установку (Сценарий A) с миграцией данных.

---

## Пошаговая инструкция

### Этап 1: Подготовка конфигурационных файлов

#### 1.1 Клонирование репозитория

```bash
git clone <repository-url> mikrotik-infrastructure
cd mikrotik-infrastructure
```

#### 1.2 Создание файла секретов

```bash
cp 00-secrets.rsc.template 00-secrets.rsc
```

#### 1.3 Редактирование секретов

**Откройте `00-secrets.rsc` и измените ВСЕ пароли:**

```routeros
############################################################
# SYSTEM CREDENTIALS
############################################################

# ⚠️ ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!
:global secAdminPassword "YourStrongPassword123!"

############################################################
# WIFI PASSWORDS
############################################################

# ⚠️ Используйте сложные пароли (минимум 12 символов)
:global secWiFiMainPassword "YourMainWiFiPass456!"
:global secWiFiGuestPassword "YourGuestWiFiPass789!"

# WDS Bridge (если используется)
:global secWDSPassword "YourWDSPassword321!"

############################################################
# VPN CREDENTIALS
############################################################

# SSTP VPN
:global secSSTPUsername "vpn-user"
:global secSSTPPassword "VeryStrongVPNPass111!"

# WireGuard S2S
# Сгенерируйте ключи:
# wg genkey | tee privatekey | wg pubkey > publickey
:global secWGPrivateKey "generate-with-wg-genkey"
:global secWGPublicKey "generate-with-wg-pubkey"
:global secWGPeerPublicKey "public-key-from-remote-site"

############################################################
# OPTIONAL: BGP & XRay (для продвинутых пользователей)
############################################################

# ... (заполните если используете)
```

**💡 Советы по безопасности паролей:**
- Минимум 12 символов
- Используйте комбинацию: буквы (A-Z, a-z), цифры (0-9), символы (!@#$%^&*)
- Не используйте словарные слова
- Не используйте одинаковые пароли для разных сервисов
- Рассмотрите использование менеджера паролей

#### 1.4 Редактирование конфигурации

**Откройте `00-config.rsc` и настройте под вашу сеть:**

**Обязательные изменения:**

```routeros
############################################################
# SYSTEM
############################################################

:global cfgHostname "R1-Core"              # ← ИЗМЕНИТЕ на ваше имя
:global cfgTimezone "Europe/Kiev"          # ← ИЗМЕНИТЕ на ваш часовой пояс

############################################################
# NETWORK INTERFACES
############################################################

:global cfgWanInterface "ether1"           # ← Проверьте номер WAN порта
:global cfgLANPorts [:toarray "ether2,ether3,ether4"]  # ← Ваши LAN порты
```

**Опциональные изменения (если нужно):**

```routeros
# Если хотите другие IP адреса:
:global cfgLANNetwork "192.168.1.0/24"
:global cfgLANGateway "192.168.1.1/24"

# Если используете IPv6:
:global cfgWANPublicIP "<YOUR_WAN_IP>"
:global cfgHELocalIPv6 "2001:470:6e:2b7::2/64"
:global cfgIPv6LANPrefix "2001:470:xxxx:1::/64"

# Если используете WiFi:
:global cfgWiFiCountry "ukraine"
:global cfgWiFiMainSSID "MyNetwork"

# Если используете контейнеры:
:global cfgDockerXRayImage "teddysun/xray:1.8.4"
```

#### 1.5 Проверка файлов

```bash
# Убедитесь, что все критические файлы на месте:
ls -la 00-*.rsc firewall_complete.rsc

# Должны увидеть:
# 00-config.rsc
# 00-secrets.rsc
# firewall_complete.rsc
```

---

### Этап 2: Загрузка файлов на роутер

#### 2.1 Определение IP адреса роутера

**Если новое устройство:**
- По умолчанию: `192.168.88.1`
- Подключитесь к LAN порту напрямую

**Если существующая конфигурация:**
- Используйте текущий IP адрес

#### 2.2 Копирование файлов через SCP

**Базовые модули (обязательно):**

```bash
scp 00-config.rsc admin@192.168.88.1:/
scp 00-secrets.rsc admin@192.168.88.1:/
scp 01-base.rsc admin@192.168.88.1:/
scp firewall_complete.rsc admin@192.168.88.1:/
scp 02-network.rsc admin@192.168.88.1:/
scp 03-dhcp.rsc admin@192.168.88.1:/
```

**Опциональные модули:**

```bash
# DNS-over-HTTPS
scp dns-doh.rsc admin@192.168.88.1:/

# WiFi
scp 06-wifi-unified.rsc admin@192.168.88.1:/

# VPN
scp 05-sstp-vpn.rsc admin@192.168.88.1:/
scp wireguard-s2s.rsc admin@192.168.88.1:/

# Containers
scp container-setup.rsc admin@192.168.88.1:/
scp xray/*.rsc admin@192.168.88.1:/xray/  # если есть каталог
```

#### 2.3 Проверка загрузки

```routeros
# Подключитесь к роутеру
ssh admin@192.168.88.1

# Проверьте файлы
/file print

# Должны увидеть все .rsc файлы
```

---

### Этап 3: Импорт конфигурации

#### 3.1 Важность порядка импорта

**⚠️ КРИТИЧЕСКИ ВАЖНО: Импортируйте файлы в ПРАВИЛЬНОМ порядке!**

Модули зависят друг от друга:
1. `00-config.rsc` - определяет переменные
2. `00-secrets.rsc` - определяет секреты
3. Все остальные модули используют эти переменные

#### 3.2 Базовая конфигурация (обязательно)

```routeros
# ====================================================
# ОБЯЗАТЕЛЬНЫЙ ПОРЯДОК!
# ====================================================

# 1. Конфигурационные переменные
/import 00-config.rsc
# Ожидаемый вывод: "Configuration variables loaded from 00-config.rsc"

# 2. Секреты
/import 00-secrets.rsc
# Ожидаемый вывод: "Secret variables loaded from 00-secrets.rsc"

# 3. Базовая система
/import 01-base.rsc
# Настроит: hostname, timezone, NTP, users, services

# 4. Firewall (КРИТИЧЕСКИ ВАЖНО!)
/import firewall_complete.rsc
# Применит все правила firewall

# 5. Сетевая инфраструктура
/import 02-network.rsc
# Настроит: bridge, VLANs, IPv6 (если настроено)

# 6. DHCP серверы
/import 03-dhcp.rsc
# Настроит DHCP для LAN, Management, Guest
```

#### 3.3 Опциональные модули

```routeros
# DNS-over-HTTPS (рекомендуется)
/import dns-doh.rsc

# WiFi (если нужен)
/import 06-wifi-unified.rsc

# SSTP VPN (если нужен)
/import 05-sstp-vpn.rsc

# WireGuard Site-to-Site (если нужен)
/import wireguard-s2s.rsc

# Container infrastructure (для продвинутых)
/import container-setup.rsc
/import xray/01-container-xray.rsc
/import xray/02-container-nginx.rsc
/import xray/03-container-certbot.rsc
/import xray/04-bgp-proxy.rsc
```

#### 3.4 Мониторинг импорта

**Во время импорта:**

```routeros
# Следите за логами в реальном времени
/log print follow where topics~"script|error|critical"

# Или после импорта:
/log print where topics~"error|critical"
```

**Что вы должны увидеть:**
```
# ✅ Хорошие сообщения:
Configuration variables loaded from 00-config.rsc
Secret variables loaded from 00-secrets.rsc
Base system configuration applied
Firewall rules applied
...

# ❌ Плохие сообщения:
syntax error (line X column Y)
bad command name <variable> (line X)
```

---

### Этап 4: Первичная проверка

#### 4.1 Проверка системы

```routeros
# Hostname и время
/system identity print
/system clock print

# NTP синхронизация
/system ntp client print
# status должен быть "synchronized"
```

#### 4.2 Проверка пользователей и сервисов

```routeros
# Проверка пользователя admin
/user print
# password должен быть скрыт (не показывается)

# Проверка сервисов
/ip service print
# Должны быть включены: ssh, winbox (с ACL)
# Должны быть ВЫКЛЮЧЕНЫ: www, www-ssl, telnet, ftp
```

#### 4.3 Проверка сети

```routeros
# Интерфейсы
/interface print
# Должны увидеть: ethers, bridge-lan, vlan20, vlan30, vlan99

# IP адреса
/ip address print
# Должен быть IP на bridge-lan (192.168.1.1/24)

# Маршруты
/ip route print
# Должен быть default route через WAN
```

#### 4.4 Проверка Firewall

```routeros
# Количество правил
/ip firewall filter print count-only
# Должно быть ~30-50 правил

/ip firewall nat print count-only
# Должно быть ~5-10 правил

# Проверка критических правил
/ip firewall filter print where comment~"Drop|Block"
```

#### 4.5 Проверка DHCP

```routeros
# DHCP серверы
/ip dhcp-server print
# Должны быть: dhcp-lan, dhcp-mgmt, dhcp-guest

# Pools
/ip pool print

# Leases (если есть клиенты)
/ip dhcp-server lease print
```

---

## Проверка после развёртывания

### Тест-план

#### Тест 1: Подключение клиента к LAN

1. Подключите компьютер к LAN порту
2. Проверьте получение IP адреса:
   ```bash
   # Windows
   ipconfig

   # Linux/Mac
   ip a
   ```
3. Должны получить IP из диапазона `192.168.1.50-192.168.1.250`
4. Gateway должен быть `192.168.1.1`

#### Тест 2: Доступ в интернет

```bash
# Ping роутера
ping 192.168.1.1

# Ping публичного DNS
ping 1.1.1.1

# DNS резолвинг
ping google.com
nslookup google.com

# Проверка HTTP/HTTPS
curl -I https://google.com
```

#### Тест 3: Firewall защита

**С клиента в LAN:**

```bash
# Попытка доступа к Management VLAN (должно блокироваться если не разрешено)
ping 172.16.99.1

# SSH к роутеру (должно работать из разрешённых сетей)
ssh admin@192.168.1.1
```

**С клиента в Guest VLAN (если настроен):**

```bash
# Попытка доступа к LAN (должно блокироваться)
ping 192.168.1.10
```

#### Тест 4: Управление роутером

```bash
# SSH доступ
ssh admin@192.168.1.1
# Должен запросить пароль из secAdminPassword

# WinBox доступ
# Откройте WinBox, подключитесь к 192.168.1.1
```

#### Тест 5: DNS-over-HTTPS (если установлен)

```routeros
# На роутере
/ip dns print
# use-doh-server: https://cloudflare-dns.com/dns-query
# doh-server-verified-count: должно быть >0

# Тест резолвинга
:resolve google.com
```

#### Тест 6: WiFi (если установлен)

1. Найдите SSID в списке сетей
2. Подключитесь с паролем из `secWiFiMainPassword`
3. Проверьте получение IP адреса
4. Проверьте доступ в интернет

---

## Rollback план

### Когда использовать rollback:

- ❌ Конфигурация не импортируется (syntax errors)
- ❌ Потеряли доступ к роутеру
- ❌ Firewall блокирует весь трафик
- ❌ Критические сервисы не работают

### Метод 1: Restore из резервной копии

```routeros
# Просмотр доступных backup
/file print where name~".backup"

# Восстановление
/system backup load name=backup-before-v1.3

# Роутер перезагрузится автоматически
```

**⚠️ ВНИМАНИЕ:** Все изменения после backup будут потеряны!

### Метод 2: Safe Mode

**Если вы ещё подключены к роутеру:**

```routeros
# При каждом изменении используйте Safe Mode
# Нажмите Ctrl+X в терминале или используйте команду
[Ctrl+X]

# Safe Mode откатит изменения если соединение потеряно
```

### Метод 3: Reset кнопка (крайняя мера)

**Только если потеряли весь доступ:**

1. Нажмите и держите кнопку **RESET**
2. Подождите 5 секунд
3. Роутер сбросится к заводским настройкам
4. Подключитесь к `192.168.88.1` (дефолтный IP)
5. Логин: `admin`, пароль: пустой
6. Загрузите резервную копию

---

## Обновление с предыдущих версий

### Миграция с v1.2 → v1.3

#### Что изменилось:
- Введена централизованная конфигурация (00-config.rsc, 00-secrets.rsc)
- Все `:local` переменные в модулях заменены на `:global`
- CRIT-01 и CRIT-05 решены

#### Шаги миграции:

**1. Экспорт текущих настроек**

```routeros
# Экспорт для reference
/export file=v1.2-export

# Резервная копия
/system backup save name=v1.2-backup
```

**2. Создание 00-config.rsc**

Перенесите значения из ваших текущих модулей в `00-config.rsc`.

**Пример:** Если в `01-base.rsc` было:
```routeros
:local hostname "R1-Core"
```

В `00-config.rsc` должно быть:
```routeros
:global cfgHostname "R1-Core"
```

**3. Создание 00-secrets.rsc**

Перенесите все пароли и секреты:
- Пароль admin
- WiFi пароли
- VPN credentials
- Ключи WireGuard
- BGP secrets (если есть)

**4. Импорт новой конфигурации**

```routeros
# Следуйте стандартной процедуре импорта
/import 00-config.rsc
/import 00-secrets.rsc
/import 01-base.rsc
# ... остальные модули
```

**5. Проверка**

Сравните экспорты:
```routeros
/export file=v1.3-export

# Скачайте оба файла и сравните (diff)
```

---

### Миграция с v1.0/1.1 → v1.3

**Рекомендация:** Выполните чистую установку v1.3.

**Причины:**
- Много критических изменений (firewall полностью переписан)
- Новая архитектура (централизованная конфигурация)
- Проще и безопаснее начать с нуля

**Если нужно сохранить данные:**
1. Экспортируйте DHCP leases
2. Экспортируйте static routes (если есть)
3. Запишите custom firewall rules (если есть)
4. Выполните чистую установку v1.3
5. Вручную добавьте сохранённые данные

---

## Post-deployment задачи

### Обязательные:

1. **Изменить пароль admin**
   ```routeros
   /user set admin password="$secAdminPassword"
   ```

2. **Создать регулярные backup**
   ```routeros
   # Вручную
   /system backup save name=mikrotik-$(date "+%Y%m%d")

   # Или настройте Scheduler (рекомендуется)
   /system scheduler add \
       name=daily-backup \
       interval=1d \
       on-event="/system backup save name=auto-backup"
   ```

3. **Настроить мониторинг логов**
   ```routeros
   # Регулярно проверяйте критические события
   /log print where topics~"error|critical"
   ```

### Рекомендуемые:

1. **Документировать вашу конфигурацию**
   - Сохраните кастомные изменения в 00-config.rsc
   - Ведите changelog своих изменений

2. **Протестировать failover**
   - Отключите WAN, проверьте поведение
   - Перезагрузите роутер, проверьте что всё поднимается

3. **Настроить удалённый backup**
   - Периодически копируйте backup на внешнее хранилище
   - Рассмотрите автоматизацию (SCP, FTP, email)

---

## Troubleshooting развёртывания

См. подробное руководство: [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)

### Частые проблемы:

| Проблема | Причина | Решение |
|----------|---------|---------|
| Syntax error при импорте | Не импортирован 00-config.rsc | Импортируйте сначала 00-config.rsc |
| "bad command name $cfg..." | Переменная не определена | Проверьте импорт 00-config.rsc |
| Нет интернета | WAN не настроен | Проверьте /ip address и /ip route |
| Не могу подключиться по SSH | ACL блокирует | Временно: /ip service set ssh address=0.0.0.0/0 |
| Firewall блокирует всё | Правила в неправильном порядке | Проверьте firewall_complete.rsc |

---

## Контакты и поддержка

- **Документация:** [docs/README.md](./README.md)
- **Быстрый старт:** [QUICK_START.md](./QUICK_START.md)
- **Архитектура:** [ARCHITECTURE.md](./ARCHITECTURE.md)
- **MikroTik Wiki:** [wiki.mikrotik.com](https://wiki.mikrotik.com)

---

**Создано:** Claude Code (Sonnet 4.5)
**Дата:** 15 декабря 2025
**Версия:** 1.3
